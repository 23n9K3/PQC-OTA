# 固件数字签名与发布者身份认证说明

## 1. 实现目标

本次改造在现有 STM32L4R5 A/B OTA 工程中增加 ML-DSA-65 数字签名验证。只引入 `ML-DSA-TEST\ML-DSA` 工程中 `MLDSA_CLEAN_M4` 所用的纯 C 核心，未引入其他 3 个 target，也未引入 M4F/ASM 实现。

改造后，只有持有受信任发布者私钥的环境才能生成 Bootloader 接受的 OTA 包。替换或修改 `app_b.bin`、版本、发布者或签名，都会导致验证失败。

## 2. 升级前后的变化

### 2.1 原 OTA 的工作方式和局限

升级前的 `.ota` 由 Header 和 APP_B Payload 组成。Header 中保存 Payload 的 SHA-256，Bootloader 在接收完成后重新计算 SHA-256，用于发现传输损坏或数据丢失。

SHA-256 只能证明“收到的固件和 Header 中记录的 hash 一致”，不能证明“固件是由受信任发布者制作的”。如果攻击者替换 APP_B，同时重新计算 SHA-256 并改写 Header，原流程无法区分这个伪造包。

### 2.2 升级后新增的功能

| 新增功能 | 作用 |
| --- | --- |
| ML-DSA-65 数字签名 | 发布端使用私钥签名，Bootloader 使用公钥验签 |
| 发布者身份认证 | 只接受 Bootloader 内置的 `publisher_id` 和受信任公钥 |
| Key ID 公钥绑定 | 使用公钥 SHA-256 的前 16 字节标识密钥，防止使用错误公钥 |
| OTA v2 签名容器 | 在 Header 和 Payload 之间新增 3309 字节 ML-DSA-65 签名 |
| 签名与固件内容绑定 | Header 中包含 Payload SHA-256，对 Header 签名即绑定了完整固件 |
| 先认证、后擦除 | 签名验证通过前不擦除 APP_B，非法包不会破坏已有 APP_B |
| 非法包明确拒绝 | 发布者或签名失败时返回 OTA ERROR code 14 |
| PC 端签名工具链 | 新增密钥生成、签名打包、离线验签、UART 发送和篡改回归测试 |
| Bootloader 后量子验签 | 将 `MLDSA_CLEAN_M4` 纯 C 核心编译进 Bootloader，不依赖 M4F/ASM target |

升级后的安全判断是：

```text
格式正确
  AND 发布者ID正确
  AND Key ID正确
  AND ML-DSA-65签名正确
  AND Payload SHA-256正确
  => 允许安装并切换到APP_B
```

## 3. 信任模型

```text
发布环境（保密）                         STM32 Bootloader
ML-DSA-65 私钥  --签名--> firmware.ota  --UART-->  发布者 ID + Key ID + ML-DSA 公钥
                                                        |
                                                        +-- 验签通过后才擦除/写入 APP_B
```

- 私钥只用于 PC/离线发布环境，不会编译进 Bootloader。
- Bootloader 只内置公钥、发布者 ID 和 Key ID。
- Key ID 为 `SHA-256(public_key)[0:16]`，用于精确选择信任公钥。
- 身份字段采用常数时间比较；ML-DSA 使用固定上下文 `UPQC-OTA-SIGN-V2`。

## 4. OTA v2 格式与签名原理

```text
偏移 0                     100                         3409
     +----------------------+----------------------------+------------------+
     | OTA v2 Header (100B) | ML-DSA-65 Signature (3309B)| APP_B Payload    |
     +----------------------+----------------------------+------------------+
```

Header 包含 magic、格式版本、固件版本、Payload 长度、Payload SHA-256、发布者 ID、Key ID、签名算法、签名长度和 Header CRC32。

ML-DSA 签名对象是完整的 100 字节 Header。Header 包含 Payload SHA-256，所以签名绑定了发布者、密钥、版本和整个 APP_B 内容。Header CRC32 只用于快速检测传输错误，安全性由 ML-DSA 和 SHA-256 提供。

为了在每次启动时都能重新验签，每个 APP slot 最后一个 4 KB Flash Page 作为持久 Manifest：

```text
APP_A image     : 0x08020000 - 0x080FEFFF，最大0xDF000字节
APP_A Manifest  : 0x080FF000 - 0x080FFFFF，4096字节
APP_B image     : 0x08100000 - 0x081DEFFF，最大0xDF000字节
APP_B Manifest  : 0x081DF000 - 0x081DFFFF，4096字节
Metadata        : 0x081E0000 - 0x081FFFFF
```

Manifest 页中保存 100 字节 Header、4 字节对齐填充和 3309 字节签名。签名从页内偏移 104 开始，以满足 STM32 Flash 8 字节编程对齐。

### 4.1 签名覆盖关系

```text
APP_B Payload
      |
      +-- SHA-256 ---------------------------+
                                             |
发布者ID + Key ID + 固件版本 + 长度 + Payload Hash
                                             |
                                             +-- 组成OTA Header
                                                        |
发布者私钥 ----------------------------------+-- ML-DSA-65签名
```

因此，下列任意内容被改动后都无法通过完整验证：

- APP_B 任意一个字节；
- Payload 长度或固件版本；
- `publisher_id` 或 `key_id`；
- ML-DSA 签名本身；
- Header 中的 Payload SHA-256。

### 4.2 密钥的作用

- **私钥**：用于制作签名，只能保存在发布端。拥有私钥等价于拥有发布固件的权限。
- **公钥**：只用于验证签名，内置在 Bootloader 中，不能用来伪造签名。
- **发布者 ID**：表示签名者的逻辑身份。
- **Key ID**：表示具体哪一把公钥，为未来的密钥轮换和多发布者扩展提供基础。

## 5. 升级后的完整 OTA 工作流程

### 5.1 PC 端发布流程

1. Keil 编译 `APP_B` target，链接地址为 `0x08100000`。
2. `fromelf` 将 `APP_B.axf` 转换为 `app_b.bin`。
3. `pack.py` 计算 `SHA-256(app_b.bin)`。
4. 工具将固件版本、长度、hash、发布者 ID、Key ID 和算法信息填入 100 字节 Header，然后计算 Header CRC32。
5. `mldsa_tool.exe` 使用发布者 ML-DSA-65 私钥和上下文 `UPQC-OTA-SIGN-V2` 对完整 Header 签名。
6. `pack.py` 立即用对应公钥反向验签，确认签名有效后才写出 `firmware.ota`。
7. `verify_ota.py` 可在发送前再次检查包结构、Payload hash、Key ID 和签名。

### 5.2 UART 传输和 Bootloader 认证流程

1. 复位后 Bootloader 在 1 秒窗口内等待 `u`。
2. `send_uart.py` 检测到 Bootloader 提示后自动发送 `u`，进入 OTA 模式。
3. PC 发送 START 帧和 OTA 总长度；Bootloader 确认总长度可容纳于 APP_B 分区。
4. PC 发送 100 字节 Header；Bootloader 检查 magic、OTA v2、固件长度、算法 ID、签名长度和 Header CRC32。
5. PC 将 3309 字节签名按最大 512 字节分包发送，每包通过帧 CRC32 和序号 ACK 检测丢包、错包和乱序。
6. 签名接收完成后，Bootloader 先常数时间比较 `publisher_id` 和 `key_id`，然后使用内置公钥执行 ML-DSA-65 验签。
7. 验签失败时返回 ERROR code 14，结束本次 OTA；此时尚未擦除 APP_B。
8. 验签成功后 Bootloader 才擦除 APP_B，并回复最后一个签名分包 ACK。PC 端看到 `Publisher authenticated` 表示板端认证和 APP_B 擦除阶段已完成。
9. PC 分包发送 APP_B Payload；Bootloader 边写入 `0x08100000` 起始的 APP_B 分区，边计算 SHA-256，并对每包回复 ACK。
10. PC 发送 END；Bootloader 将实际 SHA-256 与已通过签名保护的 `firmware_hash` 比较。
11. hash 不一致时拒绝启用 APP_B；hash 一致时，Bootloader 将 Header 和签名写入 `0x081DF000` 的 APP_B Manifest 页。
12. Bootloader 直接从 Flash 重新读取 APP_B Manifest，再执行一次 ML-DSA-65 验签和 APP_B SHA-256，确认安装结果可启动。
13. 二次验证通过后才更新 Metadata，将 APP_B 标记为待确认镜像。
14. Bootloader 返回最终 ACK，PC 显示 `OTA send done`，板子在延时 200 ms 后复位。

### 5.3 重启和 APP_B 启动流程

1. 复位后 Bootloader 读取 Metadata。
2. Bootloader 按 Metadata 选择 APP_A 或 APP_B，从该 slot 的最后 4 KB 读取 Manifest。
3. 检查 Manifest 格式、发布者 ID、Key ID，并使用内置公钥验证 ML-DSA-65 签名。
4. 验签通过后，根据 Header 中的固件长度计算当前 Flash APP 镜像的 SHA-256，与已签名的 `firmware_hash` 比较。
5. 再检查 APP 向量表的 MSP 和 Reset Handler，然后设置 VTOR、MSP 并跳转 APP。
6. APP_B 的 Manifest、签名、hash 或向量表任一检查失败时，Bootloader 将 APP_B 标记为无效，再使用相同流程验证 APP_A。
7. APP_A 也失败时不跳转任何 APP，停留在 Bootloader OTA 模式。
8. APP_B 通过 Secure Boot 并启动后，应用调用 `app_confirm_boot_ok()` 清除待确认状态；未确认超过允许次数时回滚 APP_A。

## 6. 签名与验签代码在工程中的位置

签名功能分为两个安全域：PC 发布端持有私钥并生成签名；Bootloader 只持有公钥并验证签名。APP_A 和 APP_B 不参与签名或验签。

```text
PC发布端：私钥 + app_b.bin -> 生成签名 -> firmware.ota
Bootloader：公钥 + firmware.ota -> 验证签名 -> 允许/拒绝安装
APP_A/APP_B：不保存密钥，不执行ML-DSA签名或验签
```

### 6.1 PC 端生成签名的代码

| 文件 | 职责 |
| --- | --- |
| `pc_tool/pack.py` | 读取 `app_b.bin`，构造 OTA Header，调用 ML-DSA 工具签名，组成 `firmware.ota` |
| `pc_tool/mldsa_host/mldsa_tool.c` | PC 端 ML-DSA 密钥生成、签名和验签命令行入口 |
| `pc_tool/ota_format.py` | OTA v2 Header、签名长度、分包和 CRC32 格式定义 |
| `pc_tool/provision_dev_publisher.py` | 生成开发密钥，并将公钥和发布者信息生成为 Bootloader C 源码 |
| `pc_tool/verify_ota.py` | 发送前离线检查 Payload hash、Key ID 和 ML-DSA 签名 |
| `pc_tool/test_signed_ota.py` | 验证合法包通过，Header、签名和 Payload 篡改后被拒绝 |

PC 端真正生成签名的核心调用在 `pc_tool/mldsa_host/mldsa_tool.c`：

```c
PQCLEAN_MLDSA65_CLEAN_crypto_sign_signature_ctx(
    signature,
    &signature_len,
    message,
    message_len,
    k_ota_context,
    sizeof(k_ota_context) - 1U,
    sk);
```

其中 `message` 是完整的 100 字节 OTA Header，`sk` 是 ML-DSA-65 私钥，开发测试私钥位于：

```text
pc_tool/keys/dev_publisher_sk.bin
```

该私钥只用于开发联调，不会编译进 Bootloader，不能用于量产。

### 6.2 Bootloader 板端验签的代码

| 文件 | 职责 |
| --- | --- |
| `bootloader/Core/Src/ota_transport_uart.c` | 接收 Header 和签名，调用验签；验签成功后才擦除 APP_B |
| `bootloader/Core/Src/ota_signature.c` | Bootloader 验签业务入口，检查发布者和算法后调用 ML-DSA-65 |
| `bootloader/Core/Src/secure_boot.c` | 每次跳转前读取 slot Manifest，重新验签并计算 APP Flash SHA-256 |
| `bootloader/Core/Src/trusted_publisher.c` | 常数时间比较 `publisher_id` 和 `key_id` |
| `bootloader/Core/Src/trusted_publisher_key.c` | 保存受信任发布者 ID、Key ID 和 1952 字节公钥，不包含私钥 |
| `bootloader/Core/Src/mldsa_verify_randombytes.c` | 为 Bootloader 中不允许使用的签名/密钥生成路径提供失败关闭的随机数桩 |

UART 状态机在签名接收完成后执行：

```c
if (!ota_signature_verify(&header, ota_signature_buffer)) {
    printf("[SEC] Publisher authentication FAILED\r\n");
    uart_send_status(huart, OTA_CMD_ERROR, frame.seq, 14U);
    return 0;
}
```

`ota_signature_verify()` 内的 ML-DSA-65 核心验签调用为：

```c
PQCLEAN_MLDSA65_CLEAN_crypto_sign_verify_ctx(
    signature,
    header->signature_size,
    (const uint8_t *)header,
    sizeof(*header),
    ota_signature_context,
    sizeof(ota_signature_context) - 1U,
    g_trusted_publisher_public_key);
```

返回 0 表示签名正确。只有该调用成功后，UART 状态机才会执行：

```c
flash_if_erase_app_b();
```

### 6.3 Bootloader Middleware 的职责

Bootloader Keil 工程中的 Middleware 分组为：

```text
Middleware
└── ML-DSA-65
    └── CLEAN_M4
        ├── sign.c
        ├── packing.c
        ├── poly.c
        ├── polyvec.c
        ├── ntt.c
        ├── reduce.c / rounding.c
        └── symmetric-shake.c / fips202.c
```

对 Bootloader 的实际运行流程而言，这个 Middleware **只负责验签**：

```text
ota_transport_uart.c
  -> ota_signature_verify()
  -> PQCLEAN_MLDSA65_CLEAN_crypto_sign_verify_ctx()
  -> SHAKE / 多项式 / NTT 等ML-DSA底层运算
```

需要区分“引入的上游源码包含什么”与“Bootloader 实际调用什么”：

- 上游 `sign.c` 同时定义了密钥生成、签名和验签 API；
- Bootloader 业务代码只调用 `crypto_sign_verify_ctx()`；
- Bootloader 不生成密钥，不持有私钥，不生成固件签名；
- ARMCC 链接器会按函数段移除未被 Bootloader 引用的密钥生成和签名代码；
- 即使未来错误调用签名或密钥生成接口，`mldsa_verify_randombytes.c` 也会返回失败，不会产生可用随机数。

因此当前工程的安全职责是：

```text
PC发布端：保存私钥，生成ML-DSA-65签名
Bootloader：保存公钥，验证ML-DSA-65签名
APP_A/APP_B：执行应用逻辑和启动确认，不处理签名密钥
```

Bootloader 使用独立 startup，将栈扩大为 `0x18000`，以满足 ML-DSA-65 验签临时数组的需求。

## 7. 开发环境与离线验证

首次生成开发发布者：

```powershell
cd C:\Users\3nigma\Desktop\armPrj\μPQC-OTA_1\pc_tool
powershell -ExecutionPolicy Bypass -File .\build_mldsa_tool.ps1
python .\provision_dev_publisher.py --force
```

上述命令会改变 Bootloader 内置公钥，必须重新编译和烧录 Bootloader。同一发布者正常发版时不再执行 `--force`。

生成并检查 OTA：

```powershell
& "D:\Keil\ARM\ARMCC\bin\fromelf.exe" --bin --output .\app_b.bin ..\app\MDK-ARM\APP_B\APP_B.axf
python .\pack.py --input .\app_b.bin --output .\firmware.ota --manifest-output .\app_b_manifest.bin --version 2
python .\verify_ota.py --file .\firmware.ota
python .\verify_manifest.py --manifest .\app_b_manifest.bin --app .\app_b.bin
python .\test_signed_ota.py
python .\test_secure_boot.py
```

预期输出：

```text
[VERIFY] PASS
[VERIFY] signature : ML-DSA-65 valid
[TEST] PASS: valid signed OTA accepted
[TEST] PASS: tampered header rejected
[TEST] PASS: tampered signature rejected
[TEST] PASS: tampered payload rejected
[SECURE-BOOT TEST] PASS: valid app_a.bin accepted
[SECURE-BOOT TEST] PASS: valid app_b.bin accepted
[SECURE-BOOT TEST] PASS: tampered APP rejected
[SECURE-BOOT TEST] PASS: tampered Manifest rejected
[SECURE-BOOT TEST] PASS: mismatched image/Manifest rejected
```

## 8. 板端验证步骤

### 8.1 正常签名升级

1. 编译 Bootloader、APP_A 和 APP_B，确认均为 `0 Error(s)`。APP_A/APP_B 的 IROM1 Size 必须是 `0x000DF000`。
2. 生成 APP_A 的首次启动 Manifest：

```powershell
& "D:\Keil\ARM\ARMCC\bin\fromelf.exe" --bin --output .\app_a.bin ..\app\MDK-ARM\APP_A\APP_A.axf
python .\pack.py --input .\app_a.bin --output .\app_a_signed.ota --manifest-output .\app_a_manifest.bin --version 1
python .\verify_manifest.py --manifest .\app_a_manifest.bin --app .\app_a.bin
```

3. 用 STM32CubeProgrammer 的 sector erase 依次烧录：

| 文件 | 烧录地址 |
| --- | --- |
| `bootloader/MDK-ARM/bootloader/bootloader.hex` | HEX 自带地址 `0x08000000` |
| `app/MDK-ARM/APP_A/APP_A.hex` | HEX 自带地址 `0x08020000` |
| `pc_tool/app_a_manifest.bin` | 手工指定 `0x080FF000` |

4. 不要使用 Full Chip Erase；单独重新烧录 APP_A 时也不要擦除 `0x080FF000` Manifest 页，除非同时烧录新 Manifest。
5. 启动板子，预期首先出现：

```text
[BOOT] Try boot App A
[SECURE BOOT] Verify app at 0x08020000
[SECURE BOOT] PASS publisher=UPQC-PUBLISHER01 version=1 size=...
[BOOT] Jump to app: 0x08020000
```

6. 关闭占用 COM3 的 fireTools 或其他串口助手。
7. 在 `pc_tool` 目录执行 APP_B OTA：

```powershell
python .\send_uart.py --port COM3 --baud 115200 --timeout 60 --file .\firmware.ota
```

8. 按板子 RESET。脚本检测提示后自动发送 `u`。
9. 升级完成后的预期日志：

```text
[OTA] Signed Header ok ...
[SEC] Verify ML-DSA-65 publisher signature
[SEC] Publisher authentication OK
[FLASH] Erase App B
[SECURE BOOT] Write App B Manifest
[SECURE BOOT] Verify app at 0x08100000
[SECURE BOOT] PASS publisher=UPQC-PUBLISHER01 version=2 size=...
[OTA] OTA SUCCESS
[OTA] Reboot to App B
```

10. 再次手动 RESET，不进入 OTA。预期每次启动都出现 APP_B Secure Boot PASS，然后才跳转 APP_B。

### 8.2 板端篡改拒绝

1. 复制 `firmware.ota`，用二进制编辑器改动签名区任意 1 字节（偏移 100–3408）。
2. 发送被篡改的 OTA。
3. 预期日志为 `[SEC] Publisher authentication FAILED`，PC 显示 `ERROR code=14`。
4. 因为验签在擦除之前执行，APP_B 应保持不变。

### 8.3 每次启动验签的板端篡改测试

测试 APP_B 镜像被修改：

1. 先完成一次正常 APP_B OTA，确认重启时出现 `[SECURE BOOT] PASS`。
2. 使用 STM32CubeProgrammer 读取 APP_B 范围并保存备份。
3. 在 APP_B 可执行镜像内选择一个非向量表字节，例如 `0x08100100`，通过扇区擦除后写入一份修改过的 APP_B 测试镜像。
4. 不要改动 `0x081DF000` 的 Manifest，然后复位。
5. 预期 APP_B 验签虽通过，但镜像 hash 不一致，日志为：

```text
[SECURE BOOT] Verify app at 0x08100000
[SECURE BOOT] App SHA-256 mismatch
[SECURE BOOT] App B rejected, fallback App A
[SECURE BOOT] Verify app at 0x08020000
[SECURE BOOT] PASS ...
[BOOT] Jump to app: 0x08020000
```

测试 APP_B Manifest 被修改：

1. 恢复正常 APP_B 和 Manifest。
2. 备份后擦除 `0x081DF000` 所在的 4 KB Page，将签名区任意一字节改变后重新烧录。
3. 复位，预期日志为 `[SECURE BOOT] Publisher signature invalid`，然后回退并验证 APP_A。
4. 测试完成后恢复原 APP_B 或重新执行签名 OTA。

## 9. 已实现能力与安全边界

当前已同时完成 OTA 安装前的发布者认证，以及每次跳转 APP_A/APP_B 前的 Secure Boot 验签和全镜像 SHA-256 检查。当前仍未实现 Bootloader 自身的硬件写保护、防降级和固件加密。

| 能力 | 当前状态 |
| --- | --- |
| OTA 包 ML-DSA-65 签名 | 已实现 |
| 发布者 ID 和公钥 Key ID 认证 | 已实现 |
| Payload SHA-256 完整性检查 | 已实现 |
| 验签成功后才擦除 APP_B | 已实现 |
| UART 分包 CRC32、序号和 ACK | 已实现 |
| Metadata 选择 APP_A/APP_B 及启动回滚 | 已实现 |
| APP_A/APP_B 持久签名 Manifest | 已实现，每 slot 保留 4 KB |
| 每次启动前重新验证 APP 数字签名 | 已实现 |
| 每次启动前重新计算 APP SHA-256 | 已实现 |
| Bootloader 自身完整性和 Flash 写保护 | 未实现 |
| 固件版本单调和防降级 | 未实现 |
| AES-GCM 固件加密 | 未实现 |
| 掉电续传和多副本 Metadata | 未实现 |

生产化建议：

- 使用离线签名机或 HSM 生成和保管私钥；开发私钥不得用于量产。
- 只将公钥注入 Bootloader，启用 WRP/PCROP/RDP 保护信任锚和 Bootloader。
- 后续增加版本单调计数器，拒绝有效签名但版本过旧的固件。
- 若要防止 SWD 直接改写 APP，还需实现 Secure Boot 并锁定调试口。
